interface:
  display_name: "Weftos"
  short_description: "Repo-specific patterns and workflows for weftos"
  default_prompt: "Use the weftos repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true