{
  "mcpServers": {
    "claude-flow": {
      "command": "npx",
      "args": [
        "@claude-flow/cli@latest",
        "mcp",
        "start"
      ],
      "env": {
        "CLAUDE_FLOW_MODE": "v3",
        "CLAUDE_FLOW_HOOKS_ENABLED": "true",
        "CLAUDE_FLOW_TOPOLOGY": "hierarchical-mesh",
        "CLAUDE_FLOW_MAX_AGENTS": "15",
        "CLAUDE_FLOW_MEMORY_BACKEND": "hybrid"
      },
      "autoStart": false
    },
    "cognitum": {
      "type": "sse",
      "url": "https://cognitum.one/mcpSse",
      "description": "Cognitum Cloud control plane (api.cognitum.one). 7 cloud MCP tools: catalog, orders, witness chain, etc. Add COGNITUM_API_KEY for write operations.",
      "env": {
        "COGNITUM_API_KEY": "${COGNITUM_API_KEY}"
      },
      "autoStart": false
    },
    "cognitum-seed": {
      "type": "sse",
      "url": "https://cognitum.local:8443/mcp",
      "description": "Local Cognitum Seed appliance (114 on-device tools). Requires paired bearer token. Falls back to USB-gadget at http://169.254.42.1/mcp (no auth, physical cable trust).",
      "env": {
        "COGNITUM_SEED_TOKEN": "${COGNITUM_SEED_TOKEN}"
      },
      "autoStart": false
    }
  }
}